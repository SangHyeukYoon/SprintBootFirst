language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI Server Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# Mail when CI is completed.
notifications:
  email:
    recipients:
      - nyoonsh97@gmail.com
